*** Begin Patch
*** Update File: apps/web/app/(protected)/admin/dashboard/page.tsx
@@
-import { Menu, X, Search, Bell, User, Users, Euro, Activity, Settings } from "lucide-react";
+import { Menu, X, Search, Bell, User, Users, Euro, Activity, Settings } from "lucide-react";
+import { Card, CardContent, CardHeader, CardTitle } from "@repo/design/components/Card";
+import { Button } from "@repo/design/components/Button";
@@
-            {kpi.map((item) => (
-              <div key={item.label} className={`${card} p-4`}>
-                <div className="flex items-center justify-between">
-                  <div className="text-sm text-neutral-400">{item.label}</div>
-                  <item.icon className={`size-4 ${accent}`} />
-                </div>
-                <div className="mt-2 text-2xl font-semibold">{item.value}</div>
-                <div className="mt-1 text-xs text-emerald-400">{item.diff}</div>
-              </div>
-            ))}
+            {kpi.map((item) => (
+              <Card key={item.label} className="p-4">
+                <div className="flex items-center justify-between">
+                  <div className="text-sm text-muted-foreground">{item.label}</div>
+                  <item.icon className="size-4 text-amber-400" />
+                </div>
+                <div className="mt-2 text-2xl font-semibold">{item.value}</div>
+                <div className="mt-1 text-xs text-emerald-400">{item.diff}</div>
+              </Card>
+            ))}
@@
-            <div className={`${card} p-4`}>
-              <div className="font-medium">Usuarios recientes</div>
-              <div className="mt-3 border border-neutral-800/70 rounded-lg overflow-hidden">
+            <Card className="p-4">
+              <CardHeader className="p-0">
+                <CardTitle className="font-medium">Usuarios recientes</CardTitle>
+              </CardHeader>
+              <CardContent className="p-0 mt-3 rounded-lg overflow-hidden border border-border">
                 <table className="w-full text-sm">
                   <thead className="bg-neutral-900/50 text-neutral-400">
                     <tr>
                       <th className="text-left px-3 py-2 font-normal">Nombre</th>
                       <th className="text-left px-3 py-2 font-normal">Email</th>
                       <th className="text-left px-3 py-2 font-normal">Rol</th>
                       <th className="text-left px-3 py-2 font-normal">Plan</th>
                     </tr>
                   </thead>
                   <tbody>
                     {recientes.map((u, i) => (
                       <tr key={i} className="border-t border-neutral-800/70">
                         <td className="px-3 py-2">{u.name}</td>
                         <td className="px-3 py-2 text-neutral-400">{u.email}</td>
                         <td className="px-3 py-2">
                           <span className="px-2 py-1 rounded-md bg-neutral-800 text-neutral-300 capitalize">
                             {u.role}
                           </span>
                         </td>
                         <td className="px-3 py-2">{u.plan}</td>
                       </tr>
                     ))}
                   </tbody>
                 </table>
-              </div>
-              <button className="mt-3 w-full py-2 rounded-lg bg-amber-500 text-black font-medium hover:bg-amber-400 transition">
-                Ver todos
-              </button>
-            </div>
+              </CardContent>
+              <CardFooter className="p-0 mt-3">
+                <Button className="w-full" variant="primary">Ver todos</Button>
+              </CardFooter>
+            </Card>
*** End Patch
